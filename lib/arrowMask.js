// Inline style that masks an element with the arrow icon so it inherits
// `currentColor` (see the `.arrow` rules in the component CSS modules).
export const arrowMask = {
  WebkitMaskImage: "url(/arrow-16.svg)",
  maskImage: "url(/arrow-16.svg)",
};
